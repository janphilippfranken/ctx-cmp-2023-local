model:
  cache_dir: .cache/
  model_name_or_path: bigscience/bloomz-560m
  tokenizer_name: null
  use_fast_tokenizer: true
data:
  cache_dir: .cache/
  dataset_name: stas/openwebtext-10k
  load_split: train
  num_proc: 4
  batched: true
  batch_size: 1000
  remove_columns:
  - text
  val_size: 0.2
